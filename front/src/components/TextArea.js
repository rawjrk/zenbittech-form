import styled from "styled-components";
import { inputStyles } from "../styles/templates";

const TextArea = styled.textarea`
  padding: 2em 2.5em;
  height: 189px;
  ${inputStyles}
`;

export default TextArea;
